# Canonical public URL. This is the only URL normally changed per environment.
APP_URL=http://localhost:8080

# Infrastructure overrides. Leave these defaults unchanged for local development.
APP_HTTP_PORT=8080
CORS_EXTRA_ORIGINS=http://127.0.0.1:8080,http://localhost:5173,http://127.0.0.1:5173


APP_HTTP_PORT=8080
APP_URL=http://localhost:8080

# Set db url
DATABASE_URL= 

JWT_SECRET=replace-with-a-long-random-secret

# Set super admin password
SUPER_ADMIN_EMAIL=admin@example.com
SUPER_ADMIN_PASSWORD=